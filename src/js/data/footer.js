export class Footer {
    constructor(
        text,
        icon_url,
        proxy_icon_url,
    ) {
        this.text = text || null
        this.icon_url = icon_url || null
        this.proxy_icon_url = proxy_icon_url || null
    }
}
