export class Field {
    constructor(
        name,
        value,
        inline,
    ) {
        this.name = name || null
        this.value = value || null
        this.inline = inline || null
    }
}
